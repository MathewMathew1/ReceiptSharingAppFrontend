import { Receipt } from "../../types/Receipt";

const EditReceipt = ({receipt, cancelFunction}: {receipt: Receipt, cancelFunction: () => void}) => {
    console.log(receipt)
    return <div>

    </div>
}
 
export default EditReceipt;